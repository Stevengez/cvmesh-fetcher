{
  "name": "cvmesh-fetcher-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "sample": "pnpm --filter cvmesh-react-sample dev",
    "sample:build": "pnpm --filter cvmesh-react-sample build"
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "typescript": "^5.8.2"
  }
}
